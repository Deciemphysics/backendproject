include ./mixin

doctype html
html
  head
    title User List
  body
    h1 Finish this page
      ul 
        for user in users
          li
            ul
              li User:  
              span= user.name
              li Age: 
              span= user.age
              li Email
              span= user.email
          a(href="/" + user.name) Edit
          form(action='/del'+user.name, method="POST")
              input(type='submit', value='Delete')
      div
        a(href="/add") Add User